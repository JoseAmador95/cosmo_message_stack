#ifndef COSMO_MESSAGE_TYPES_H
#define COSMO_MESSAGE_TYPES_H

#include <stddef.h>
#include <stdint.h>

#define COSMO_NODE_MAX UINT16_MAX

typedef uint16_t cosmoNode;
typedef uint16_t cosmoMsgId;
typedef size_t cosmoPayloadSize;

#endif // COSMO_MESSAGE_TYPES_H